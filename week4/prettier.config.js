/** @type {import('prettier').Config} */
const config = {
  tabWidth: 2,
  useTabs: false,
  singleQuote: true,
  trailingComma: 'es5',
  bracketSpacing: true,
  arrowParens: 'always',
  // Tailwind 사용 시: plugins: ['prettier-plugin-tailwindcss'],
}
export default config